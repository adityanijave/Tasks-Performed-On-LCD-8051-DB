#include"delay_sep.h"

void delay(unsigned int time)											//delay funcation with one argument
{
	unsigned int i,j;															  //decleration of variables inside funcation
	for(i = 0; i < time; i++)												//for loop
	{
		for(j = 0; j < 1000; j++);										//nested for loop
	}
}
